-- Auto-generated by auto_prove.py. Do not edit manually.
import Mathlib.Tactic

open BigOperators

namespace AutoProved

-- stmt: theorem t1 : (1:ℕ) + 1 = 2
-- goal:
--   ⊢ 1 + 1 = 2
-- added: 2026-06-05
theorem t1 : (1:ℕ) + 1 = 2 := by
  rfl

-- stmt: theorem t2 (n : ℕ) : n + 0 = n
-- goal:
--   n : ℕ
--   ⊢ n + 0 = n
-- added: 2026-06-05
theorem t2 (n : ℕ) : n + 0 = n := by
  omega

-- stmt: theorem t3 (a b : ℤ) : a + b = b + a
-- goal:
--   a b : ℤ
--   ⊢ a + b = b + a
-- added: 2026-06-05
theorem t3 (a b : ℤ) : a + b = b + a := by
  omega

-- stmt: theorem t4 (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 2 = a ^ 2 + 2 * a * b + b ^ 2
-- added: 2026-06-05
theorem t4 (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2 := by
  ring

-- stmt: theorem t5 (a b c : ℕ) : (a + b) + c = a + (b + c)
-- goal:
--   a b c : ℕ
--   ⊢ a + b + c = a + (b + c)
-- added: 2026-06-05
theorem t5 (a b c : ℕ) : (a + b) + c = a + (b + c) := by
  omega

-- stmt: theorem t6 (n : ℕ) : 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1)
-- goal:
--   n : ℕ
--   ⊢ 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1)
-- added: 2026-06-05
theorem t6 (n : ℕ) : 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1) := by
  induction n with
  | zero => simp
  | succ m ih =>
    rw [Finset.sum_range_succ]; nlinarith [ih]

-- stmt: theorem t7 (n : ℕ) : 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1)
-- goal:
--   n : ℕ
--   ⊢ 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1)
-- added: 2026-06-05
theorem t7 (n : ℕ) : 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1) := by
  induction n with
  | zero => simp
  | succ m ih =>
    rw [Finset.sum_range_succ]; nlinarith [ih]

-- stmt: theorem bench_imp_id (P : Prop) : P → P
-- goal:
--   P : Prop
--   ⊢ P → P
-- added: 2026-06-05
theorem bench_imp_id (P : Prop) : P → P := by
  simp

-- stmt: theorem bench_imp_weak (P Q : Prop) : P → Q → P
-- goal:
--   P Q : Prop
--   ⊢ P → Q → P
-- added: 2026-06-05
theorem bench_imp_weak (P Q : Prop) : P → Q → P := by
  tauto

-- stmt: theorem bench_imp_trans (P Q R : Prop) : (P → Q) → (Q → R) → (P → R)
-- goal:
--   P Q R : Prop
--   ⊢ (P → Q) → (Q → R) → P → R
-- added: 2026-06-05
theorem bench_imp_trans (P Q R : Prop) : (P → Q) → (Q → R) → (P → R) := by
  tauto

-- stmt: theorem bench_and_intro (P Q : Prop) (hp : P) (hq : Q) : P ∧ Q
-- goal:
--   P Q : Prop
--   hp : P
--   hq : Q
--   ⊢ P ∧ Q
-- added: 2026-06-05
theorem bench_and_intro (P Q : Prop) (hp : P) (hq : Q) : P ∧ Q := by
  tauto

-- stmt: theorem bench_and_left (P Q : Prop) (h : P ∧ Q) : P
-- goal:
--   P Q : Prop
--   h : P ∧ Q
--   ⊢ P
-- added: 2026-06-05
theorem bench_and_left (P Q : Prop) (h : P ∧ Q) : P := by
  tauto

-- stmt: theorem bench_and_right (P Q : Prop) (h : P ∧ Q) : Q
-- goal:
--   P Q : Prop
--   h : P ∧ Q
--   ⊢ Q
-- added: 2026-06-05
theorem bench_and_right (P Q : Prop) (h : P ∧ Q) : Q := by
  tauto

-- stmt: theorem bench_and_comm (P Q : Prop) : P ∧ Q → Q ∧ P
-- goal:
--   P Q : Prop
--   ⊢ P ∧ Q → Q ∧ P
-- added: 2026-06-05
theorem bench_and_comm (P Q : Prop) : P ∧ Q → Q ∧ P := by
  tauto

-- stmt: theorem bench_or_inl (P Q : Prop) (hp : P) : P ∨ Q
-- goal:
--   P Q : Prop
--   hp : P
--   ⊢ P ∨ Q
-- added: 2026-06-05
theorem bench_or_inl (P Q : Prop) (hp : P) : P ∨ Q := by
  tauto

-- stmt: theorem bench_or_inr (P Q : Prop) (hq : Q) : P ∨ Q
-- goal:
--   P Q : Prop
--   hq : Q
--   ⊢ P ∨ Q
-- added: 2026-06-05
theorem bench_or_inr (P Q : Prop) (hq : Q) : P ∨ Q := by
  tauto

-- stmt: theorem bench_or_comm (P Q : Prop) : P ∨ Q → Q ∨ P
-- goal:
--   P Q : Prop
--   ⊢ P ∨ Q → Q ∨ P
-- added: 2026-06-05
theorem bench_or_comm (P Q : Prop) : P ∨ Q → Q ∨ P := by
  tauto

-- stmt: theorem bench_not_false : ¬False
-- goal:
--   ⊢ ¬False
-- added: 2026-06-05
theorem bench_not_false : ¬False := by
  simp

-- stmt: theorem bench_contrapos (P Q : Prop) : (P → Q) → (¬Q → ¬P)
-- goal:
--   P Q : Prop
--   ⊢ (P → Q) → ¬Q → ¬P
-- added: 2026-06-05
theorem bench_contrapos (P Q : Prop) : (P → Q) → (¬Q → ¬P) := by
  tauto

-- stmt: theorem bench_absurd_ex (P Q : Prop) : P → ¬P → Q
-- goal:
--   P Q : Prop
--   ⊢ P → ¬P → Q
-- added: 2026-06-05
theorem bench_absurd_ex (P Q : Prop) : P → ¬P → Q := by
  tauto

-- stmt: theorem bench_iff_refl (P : Prop) : P ↔ P
-- goal:
--   P : Prop
--   ⊢ P ↔ P
-- added: 2026-06-05
theorem bench_iff_refl (P : Prop) : P ↔ P := by
  rfl

-- stmt: theorem bench_iff_mp (P Q : Prop) (h : P ↔ Q) (hp : P) : Q
-- goal:
--   P Q : Prop
--   h : P ↔ Q
--   hp : P
--   ⊢ Q
-- added: 2026-06-05
theorem bench_iff_mp (P Q : Prop) (h : P ↔ Q) (hp : P) : Q := by
  tauto

-- stmt: theorem bench_demorgan (P Q : Prop) : ¬(P ∨ Q) → ¬P ∧ ¬Q
-- goal:
--   P Q : Prop
--   ⊢ ¬(P ∨ Q) → ¬P ∧ ¬Q
-- added: 2026-06-05
theorem bench_demorgan (P Q : Prop) : ¬(P ∨ Q) → ¬P ∧ ¬Q := by
  simp

-- stmt: theorem bench_sq_sum (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 2 = a ^ 2 + 2 * a * b + b ^ 2
-- added: 2026-06-05
theorem bench_sq_sum (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2 := by
  ring

-- stmt: theorem bench_sq_diff (a b : ℝ) : (a - b)^2 = a^2 - 2*a*b + b^2
-- goal:
--   a b : ℝ
--   ⊢ (a - b) ^ 2 = a ^ 2 - 2 * a * b + b ^ 2
-- added: 2026-06-05
theorem bench_sq_diff (a b : ℝ) : (a - b)^2 = a^2 - 2*a*b + b^2 := by
  ring

-- stmt: theorem bench_diff_sq (a b : ℝ) : (a + b) * (a - b) = a^2 - b^2
-- goal:
--   a b : ℝ
--   ⊢ (a + b) * (a - b) = a ^ 2 - b ^ 2
-- added: 2026-06-05
theorem bench_diff_sq (a b : ℝ) : (a + b) * (a - b) = a^2 - b^2 := by
  ring

-- stmt: theorem bench_cube_sum (a b : ℝ) : (a + b)^3 = a^3 + 3*a^2*b + 3*a*b^2 + b^3
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 3 = a ^ 3 + 3 * a ^ 2 * b + 3 * a * b ^ 2 + b ^ 3
-- added: 2026-06-05
theorem bench_cube_sum (a b : ℝ) : (a + b)^3 = a^3 + 3*a^2*b + 3*a*b^2 + b^3 := by
  ring

-- stmt: theorem bench_nat_comm (a b : ℕ) : a + b = b + a
-- goal:
--   a b : ℕ
--   ⊢ a + b = b + a
-- added: 2026-06-05
theorem bench_nat_comm (a b : ℕ) : a + b = b + a := by
  omega

-- stmt: theorem bench_nat_assoc (a b c : ℕ) : (a + b) + c = a + (b + c)
-- goal:
--   a b c : ℕ
--   ⊢ a + b + c = a + (b + c)
-- added: 2026-06-05
theorem bench_nat_assoc (a b c : ℕ) : (a + b) + c = a + (b + c) := by
  omega

-- stmt: theorem bench_nat_le (n : ℕ) : n ≤ n + 1
-- goal:
--   n : ℕ
--   ⊢ n ≤ n + 1
-- added: 2026-06-05
theorem bench_nat_le (n : ℕ) : n ≤ n + 1 := by
  omega

-- stmt: theorem bench_int_linarith (a b : ℤ) (h1 : a ≤ b) (h2 : b ≤ a) : a = b
-- goal:
--   a b : ℤ
--   h1 : a ≤ b
--   h2 : b ≤ a
--   ⊢ a = b
-- added: 2026-06-05
theorem bench_int_linarith (a b : ℤ) (h1 : a ≤ b) (h2 : b ≤ a) : a = b := by
  omega

-- stmt: theorem bench_sum_gauss (n : ℕ) : 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1)
-- goal:
--   n : ℕ
--   ⊢ 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1)
-- added: 2026-06-05
theorem bench_sum_gauss (n : ℕ) : 2 * ∑ k ∈ Finset.range (n + 1), k = n * (n + 1) := by
  induction n with
    | zero => simp
    | succ m ih =>
      rw [Finset.sum_range_succ]; nlinarith [ih]

-- stmt: theorem bench_sum_sq (n : ℕ) : 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1)
-- goal:
--   n : ℕ
--   ⊢ 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1)
-- added: 2026-06-05
theorem bench_sum_sq (n : ℕ) : 6 * ∑ k ∈ Finset.range (n + 1), k ^ 2 = n * (n + 1) * (2 * n + 1) := by
  induction n with
    | zero => simp
    | succ m ih =>
      rw [Finset.sum_range_succ]; nlinarith [ih]

-- stmt: theorem bench_hard_prime_inf : ∀ n : ℕ, ∃ p, n ≤ p ∧ Nat.Prime p
-- goal:
--   ⊢ ∀ (n : ℕ), ∃ p, n ≤ p ∧ Nat.Prime p
-- added: 2026-06-05
theorem bench_hard_prime_inf : ∀ n : ℕ, ∃ p, n ≤ p ∧ Nat.Prime p := by
  exact fun n => Nat.exists_infinite_primes n

-- stmt: theorem bench_hard_sqrt (a : ℝ) (ha : 0 ≤ a) : Real.sqrt (a ^ 2) = a
-- goal:
--   a : ℝ
--   ha : 0 ≤ a
--   ⊢ √(a ^ 2) = a
-- added: 2026-06-06
theorem bench_hard_sqrt (a : ℝ) (ha : 0 ≤ a) : Real.sqrt (a ^ 2) = a := by
  exact Real.sqrt_sq ha

-- stmt: theorem bench_hard_cont_comp (f g : ℝ → ℝ) (hf : Continuous f) (hg : Continuous g) : Continuous (f ∘ g)
-- goal:
--   f g : ℝ → ℝ
--   hf : Continuous f
--   hg : Continuous g
--   ⊢ Continuous (f ∘ g)
-- added: 2026-06-06
theorem bench_hard_cont_comp (f g : ℝ → ℝ) (hf : Continuous f) (hg : Continuous g) : Continuous (f ∘ g) := by
  fun_prop

-- stmt: theorem bench_hard_irrational_sqrt2 : Irrational (Real.sqrt 2)
-- goal:
--   ⊢ Irrational √2
-- added: 2026-06-06
theorem bench_hard_irrational_sqrt2 : Irrational (Real.sqrt 2) := by
  exact irrational_sqrt_two

-- stmt: theorem bench_hard_emod (a b c : ℤ) (h : c ∣ b) : a % b % c = a % c
-- goal:
--   a b c : ℤ
--   h : c ∣ b
--   ⊢ a % b % c = a % c
-- added: 2026-06-06
theorem bench_hard_emod (a b c : ℤ) (h : c ∣ b) : a % b % c = a % c := by
  exact Int.emod_emod_of_dvd a h

-- stmt: theorem sq_sum (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 2 = a ^ 2 + 2 * a * b + b ^ 2
-- added: 2026-06-07
theorem sq_sum (a b : ℝ) : (a + b)^2 = a^2 + 2*a*b + b^2 := by
  ring

-- stmt: theorem sq_diff (a b : ℝ) : (a - b)^2 = a^2 - 2*a*b + b^2
-- goal:
--   a b : ℝ
--   ⊢ (a - b) ^ 2 = a ^ 2 - 2 * a * b + b ^ 2
-- added: 2026-06-07
theorem sq_diff (a b : ℝ) : (a - b)^2 = a^2 - 2*a*b + b^2 := by
  ring

-- stmt: theorem mul_sum_diff (a b : ℝ) : (a + b) * (a - b) = a^2 - b^2
-- goal:
--   a b : ℝ
--   ⊢ (a + b) * (a - b) = a ^ 2 - b ^ 2
-- added: 2026-06-07
theorem mul_sum_diff (a b : ℝ) : (a + b) * (a - b) = a^2 - b^2 := by
  ring

-- stmt: theorem cube_sum (a b : ℝ) : (a + b)^3 = a^3 + 3*a^2*b + 3*a*b^2 + b^3
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 3 = a ^ 3 + 3 * a ^ 2 * b + 3 * a * b ^ 2 + b ^ 3
-- added: 2026-06-07
theorem cube_sum (a b : ℝ) : (a + b)^3 = a^3 + 3*a^2*b + 3*a*b^2 + b^3 := by
  ring

-- stmt: theorem cube_diff (a b : ℝ) : (a - b)^3 = a^3 - 3*a^2*b + 3*a*b^2 - b^3
-- goal:
--   a b : ℝ
--   ⊢ (a - b) ^ 3 = a ^ 3 - 3 * a ^ 2 * b + 3 * a * b ^ 2 - b ^ 3
-- added: 2026-06-07
theorem cube_diff (a b : ℝ) : (a - b)^3 = a^3 - 3*a^2*b + 3*a*b^2 - b^3 := by
  ring

-- stmt: theorem factor_diff_sq (a b : ℝ) : a^2 - b^2 = (a + b) * (a - b)
-- goal:
--   a b : ℝ
--   ⊢ a ^ 2 - b ^ 2 = (a + b) * (a - b)
-- added: 2026-06-07
theorem factor_diff_sq (a b : ℝ) : a^2 - b^2 = (a + b) * (a - b) := by
  ring

-- stmt: theorem factor_sum_cubes (a b : ℝ) : a^3 + b^3 = (a + b) * (a^2 - a*b + b^2)
-- goal:
--   a b : ℝ
--   ⊢ a ^ 3 + b ^ 3 = (a + b) * (a ^ 2 - a * b + b ^ 2)
-- added: 2026-06-07
theorem factor_sum_cubes (a b : ℝ) : a^3 + b^3 = (a + b) * (a^2 - a*b + b^2) := by
  ring

-- stmt: theorem factor_diff_cubes (a b : ℝ) : a^3 - b^3 = (a - b) * (a^2 + a*b + b^2)
-- goal:
--   a b : ℝ
--   ⊢ a ^ 3 - b ^ 3 = (a - b) * (a ^ 2 + a * b + b ^ 2)
-- added: 2026-06-07
theorem factor_diff_cubes (a b : ℝ) : a^3 - b^3 = (a - b) * (a^2 + a*b + b^2) := by
  ring

-- stmt: theorem sq_nonneg_form (a b : ℝ) : (a + b)^2 ≥ 0
-- goal:
--   a b : ℝ
--   ⊢ (a + b) ^ 2 ≥ 0
-- added: 2026-06-07
theorem sq_nonneg_form (a b : ℝ) : (a + b)^2 ≥ 0 := by
  nlinarith [sq_nonneg (a - b)]

-- stmt: theorem vertex_form_nonneg (a p q x : ℝ) (ha : 0 ≤ a) : a * (x - p)^2 + q ≥ q
-- goal:
--   a p q x : ℝ
--   ha : 0 ≤ a
--   ⊢ a * (x - p) ^ 2 + q ≥ q
-- added: 2026-06-07
theorem vertex_form_nonneg (a p q x : ℝ) (ha : 0 ≤ a) : a * (x - p)^2 + q ≥ q := by
  nlinarith [sq_nonneg (a - p), sq_nonneg (a - q), sq_nonneg (a - x), sq_nonneg (p - q), sq_nonneg (p - x), sq_nonneg (q - x), sq_nonneg (a*x - p*q)]

-- stmt: theorem my_zero_add (n : Nat) : 0 + n = n
-- goal:
--   n : ℕ
--   ⊢ 0 + n = n
-- added: 2026-06-07
theorem my_zero_add (n : Nat) : 0 + n = n := by
  omega

-- stmt: theorem my_add_assoc (a b c : Nat) : (a + b) + c = a + (b + c)
-- goal:
--   a b c : ℕ
--   ⊢ a + b + c = a + (b + c)
-- added: 2026-06-07
theorem my_add_assoc (a b c : Nat) : (a + b) + c = a + (b + c) := by
  omega

-- stmt: theorem my_add_comm (a b : Nat) : a + b = b + a
-- goal:
--   a b : ℕ
--   ⊢ a + b = b + a
-- added: 2026-06-07
theorem my_add_comm (a b : Nat) : a + b = b + a := by
  omega

-- stmt: theorem double_eq_two_mul (n : Nat) : n + n = 2 * n
-- goal:
--   n : ℕ
--   ⊢ n + n = 2 * n
-- added: 2026-06-07
theorem double_eq_two_mul (n : Nat) : n + n = 2 * n := by
  omega

-- stmt: theorem my_zero_le (n : Nat) : 0 ≤ n
-- goal:
--   n : ℕ
--   ⊢ 0 ≤ n
-- added: 2026-06-07
theorem my_zero_le (n : Nat) : 0 ≤ n := by
  omega

-- stmt: theorem even_add_even (a b : Nat) (ha : Even a) (hb : Even b) : Even (a + b)
-- goal:
--   a b : ℕ
--   ha : Even a
--   hb : Even b
--   ⊢ Even (a + b)
-- added: 2026-06-07
theorem even_add_even (a b : Nat) (ha : Even a) (hb : Even b) : Even (a + b) := by
  exact Even.add ha hb

-- stmt: theorem zmod_self (n : ℕ) : (n : ZMod n) = 0
-- goal:
--   n : ℕ
--   ⊢ ↑n = 0
-- added: 2026-06-07
theorem zmod_self (n : ℕ) : (n : ZMod n) = 0 := by
  simp

-- stmt: theorem cong_iff_dvd (a b : ℤ) (n : ℕ) : (a : ZMod n) = b ↔ (n : ℤ) ∣ b - a
-- goal:
--   a b : ℤ
--   n : ℕ
--   ⊢ ↑a = ↑b ↔ ↑n ∣ b - a
-- added: 2026-06-07
theorem cong_iff_dvd (a b : ℤ) (n : ℕ) : (a : ZMod n) = b ↔ (n : ℤ) ∣ b - a := by
  exact ZMod.intCast_eq_intCast_iff_dvd_sub a b n

-- stmt: theorem cong_add {n : ℕ} {a b : ZMod n} (h : a = b) (c : ZMod n) : a + c = b + c
-- goal:
--   n : ℕ
--   a b : ZMod n
--   h : a = b
--   c : ZMod n
--   ⊢ a + c = b + c
-- added: 2026-06-07
theorem cong_add {n : ℕ} {a b : ZMod n} (h : a = b) (c : ZMod n) : a + c = b + c := by
  exact (add_left_inj c).mpr h

-- stmt: theorem cong_mul {n : ℕ} {a b : ZMod n} (h : a = b) (c : ZMod n) : a * c = b * c
-- goal:
--   n : ℕ
--   a b : ZMod n
--   h : a = b
--   c : ZMod n
--   ⊢ a * c = b * c
-- added: 2026-06-07
theorem cong_mul {n : ℕ} {a b : ZMod n} (h : a = b) (c : ZMod n) : a * c = b * c := by
  exact ZMod.valMinAbs_inj.mp (congrArg ZMod.valMinAbs (congrFun (congrArg HMul.hMul h) c))

-- stmt: theorem zmod_prime_inv {p : ℕ} [Fact (Nat.Prime p)] (a : ZMod p) (ha : a ≠ 0) : a * a⁻¹ = 1
-- goal:
--   p : ℕ
--   inst✝ : Fact (Nat.Prime p)
--   a : ZMod p
--   ha : a ≠ 0
--   ⊢ a * a⁻¹ = 1
-- added: 2026-06-07
theorem zmod_prime_inv {p : ℕ} [Fact (Nat.Prime p)] (a : ZMod p) (ha : a ≠ 0) : a * a⁻¹ = 1 := by
  exact CommGroupWithZero.mul_inv_cancel a ha

-- stmt: theorem fermat_little {p : ℕ} [Fact (Nat.Prime p)] (a : ZMod p) (ha : a ≠ 0) : a ^ (p - 1) = 1
-- goal:
--   p : ℕ
--   inst✝ : Fact (Nat.Prime p)
--   a : ZMod p
--   ha : a ≠ 0
--   ⊢ a ^ (p - 1) = 1
-- added: 2026-06-07
theorem fermat_little {p : ℕ} [Fact (Nat.Prime p)] (a : ZMod p) (ha : a ≠ 0) : a ^ (p - 1) = 1 := by
  exact ZMod.pow_card_sub_one_eq_one ha

-- stmt: theorem fermat_little_all {p : ℕ} [hp : Fact (Nat.Prime p)] (a : ZMod p) : a ^ p = a
-- goal:
--   p : ℕ
--   hp : Fact (Nat.Prime p)
--   a : ZMod p
--   ⊢ a ^ p = a
-- added: 2026-06-07
theorem fermat_little_all {p : ℕ} [hp : Fact (Nat.Prime p)] (a : ZMod p) : a ^ p = a := by
  simp

-- stmt: theorem two_prime : Nat.Prime 2
-- goal:
--   ⊢ Nat.Prime 2
-- added: 2026-06-07
theorem two_prime : Nat.Prime 2 := by
  norm_num

-- stmt: theorem three_prime : Nat.Prime 3
-- goal:
--   ⊢ Nat.Prime 3
-- added: 2026-06-07
theorem three_prime : Nat.Prime 3 := by
  norm_num

-- stmt: theorem five_prime : Nat.Prime 5
-- goal:
--   ⊢ Nat.Prime 5
-- added: 2026-06-07
theorem five_prime : Nat.Prime 5 := by
  norm_num

-- stmt: theorem seven_prime : Nat.Prime 7
-- goal:
--   ⊢ Nat.Prime 7
-- added: 2026-06-07
theorem seven_prime : Nat.Prime 7 := by
  norm_num

-- stmt: theorem prime_ge_two (p : ℕ) (hp : Nat.Prime p) : 2 ≤ p
-- goal:
--   p : ℕ
--   hp : Nat.Prime p
--   ⊢ 2 ≤ p
-- added: 2026-06-07
theorem prime_ge_two (p : ℕ) (hp : Nat.Prime p) : 2 ≤ p := by
  exact Nat.Prime.two_le hp

-- stmt: theorem prime_odd_or_two (p : ℕ) (hp : Nat.Prime p) (hne : p ≠ 2) : p % 2 = 1
-- goal:
--   p : ℕ
--   hp : Nat.Prime p
--   hne : p ≠ 2
--   ⊢ p % 2 = 1
-- added: 2026-06-07
theorem prime_odd_or_two (p : ℕ) (hp : Nat.Prime p) (hne : p ≠ 2) : p % 2 = 1 := by
  exact (Nat.Prime.mod_two_eq_one_iff_ne_two hp).mpr hne

-- stmt: theorem prime_divisors (p : ℕ) (hp : Nat.Prime p) (k : ℕ) (hk : k ∣ p) : k = 1 ∨ k = p
-- goal:
--   p : ℕ
--   hp : Nat.Prime p
--   k : ℕ
--   hk : k ∣ p
--   ⊢ k = 1 ∨ k = p
-- added: 2026-06-07
theorem prime_divisors (p : ℕ) (hp : Nat.Prime p) (k : ℕ) (hk : k ∣ p) : k = 1 ∨ k = p := by
  exact (Nat.dvd_prime hp).mp hk

-- stmt: theorem my_gcd_dvd_left (a b : ℕ) : Nat.gcd a b ∣ a
-- goal:
--   a b : ℕ
--   ⊢ a.gcd b ∣ a
-- added: 2026-06-07
theorem my_gcd_dvd_left (a b : ℕ) : Nat.gcd a b ∣ a := by
  exact Nat.gcd_dvd_left a b

-- stmt: theorem my_gcd_dvd_right (a b : ℕ) : Nat.gcd a b ∣ b
-- goal:
--   a b : ℕ
--   ⊢ a.gcd b ∣ b
-- added: 2026-06-07
theorem my_gcd_dvd_right (a b : ℕ) : Nat.gcd a b ∣ b := by
  exact Nat.gcd_dvd_right a b

-- stmt: theorem my_dvd_gcd {k a b : ℕ} (ha : k ∣ a) (hb : k ∣ b) : k ∣ Nat.gcd a b
-- goal:
--   k a b : ℕ
--   ha : k ∣ a
--   hb : k ∣ b
--   ⊢ k ∣ a.gcd b
-- added: 2026-06-07
theorem my_dvd_gcd {k a b : ℕ} (ha : k ∣ a) (hb : k ∣ b) : k ∣ Nat.gcd a b := by
  exact Nat.dvd_gcd ha hb

-- stmt: theorem my_gcd_comm (a b : ℕ) : Nat.gcd a b = Nat.gcd b a
-- goal:
--   a b : ℕ
--   ⊢ a.gcd b = b.gcd a
-- added: 2026-06-07
theorem my_gcd_comm (a b : ℕ) : Nat.gcd a b = Nat.gcd b a := by
  exact Nat.gcd_comm a b

-- stmt: theorem my_gcd_zero_right (a : ℕ) : Nat.gcd a 0 = a
-- goal:
--   a : ℕ
--   ⊢ a.gcd 0 = a
-- added: 2026-06-07
theorem my_gcd_zero_right (a : ℕ) : Nat.gcd a 0 = a := by
  simp

-- stmt: theorem my_gcd_zero_left (a : ℕ) : Nat.gcd 0 a = a
-- goal:
--   a : ℕ
--   ⊢ Nat.gcd 0 a = a
-- added: 2026-06-07
theorem my_gcd_zero_left (a : ℕ) : Nat.gcd 0 a = a := by
  simp

-- stmt: theorem my_gcd_self (a : ℕ) : Nat.gcd a a = a
-- goal:
--   a : ℕ
--   ⊢ a.gcd a = a
-- added: 2026-06-07
theorem my_gcd_self (a : ℕ) : Nat.gcd a a = a := by
  simp

-- stmt: theorem coprime_succ (n : ℕ) : Nat.Coprime n (n + 1)
-- goal:
--   n : ℕ
--   ⊢ n.Coprime (n + 1)
-- added: 2026-06-07
theorem coprime_succ (n : ℕ) : Nat.Coprime n (n + 1) := by
  simp

-- stmt: theorem prime_coprime_of_not_dvd {p n : ℕ} (hp : Nat.Prime p) (h : ¬ p ∣ n) : Nat.Coprime p n
-- goal:
--   p n : ℕ
--   hp : Nat.Prime p
--   h : ¬p ∣ n
--   ⊢ p.Coprime n
-- added: 2026-06-07
theorem prime_coprime_of_not_dvd {p n : ℕ} (hp : Nat.Prime p) (h : ¬ p ∣ n) : Nat.Coprime p n := by
  exact (Nat.Prime.coprime_iff_not_dvd hp).mpr h

-- stmt: theorem coprime_dvd_of_dvd_mul {k m n : ℕ} (hco : Nat.Coprime k n) (h : k ∣ m * n) : k ∣ m
-- goal:
--   k m n : ℕ
--   hco : k.Coprime n
--   h : k ∣ m * n
--   ⊢ k ∣ m
-- added: 2026-06-07
theorem coprime_dvd_of_dvd_mul {k m n : ℕ} (hco : Nat.Coprime k n) (h : k ∣ m * n) : k ∣ m := by
  exact Nat.Coprime.dvd_of_dvd_mul_right hco h

-- stmt: theorem exists_prime_ge (n : ℕ) : ∃ p, n ≤ p ∧ Nat.Prime p
-- goal:
--   n : ℕ
--   ⊢ ∃ p, n ≤ p ∧ Nat.Prime p
-- added: 2026-06-07
theorem exists_prime_ge (n : ℕ) : ∃ p, n ≤ p ∧ Nat.Prime p := by
  exact Nat.exists_infinite_primes n

-- stmt: theorem choose_zero (n : ℕ) : n.choose 0 = 1
-- goal:
--   n : ℕ
--   ⊢ n.choose 0 = 1
-- added: 2026-06-07
theorem choose_zero (n : ℕ) : n.choose 0 = 1 := by
  simp

-- stmt: theorem choose_self (n : ℕ) : n.choose n = 1
-- goal:
--   n : ℕ
--   ⊢ n.choose n = 1
-- added: 2026-06-07
theorem choose_self (n : ℕ) : n.choose n = 1 := by
  simp

-- stmt: theorem choose_one (n : ℕ) : n.choose 1 = n
-- goal:
--   n : ℕ
--   ⊢ n.choose 1 = n
-- added: 2026-06-07
theorem choose_one (n : ℕ) : n.choose 1 = n := by
  simp

-- stmt: theorem choose_symm (n k : ℕ) (h : k ≤ n) : n.choose k = n.choose (n - k)
-- goal:
--   n k : ℕ
--   h : k ≤ n
--   ⊢ n.choose k = n.choose (n - k)
-- added: 2026-06-07
theorem choose_symm (n k : ℕ) (h : k ≤ n) : n.choose k = n.choose (n - k) := by
  exact Eq.symm (Nat.choose_symm h)

-- stmt: theorem pascal (n k : ℕ) : n.choose k + n.choose (k + 1) = (n + 1).choose (k + 1)
-- goal:
--   n k : ℕ
--   ⊢ n.choose k + n.choose (k + 1) = (n + 1).choose (k + 1)
-- added: 2026-06-07
theorem pascal (n k : ℕ) : n.choose k + n.choose (k + 1) = (n + 1).choose (k + 1) := by
  rfl

-- stmt: theorem my_mul_one {G : Type*} [Group G] (a : G) : a * 1 = a
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ a * 1 = a
-- added: 2026-06-07
theorem my_mul_one {G : Type*} [Group G] (a : G) : a * 1 = a := by
  simp

-- stmt: theorem my_one_mul {G : Type*} [Group G] (a : G) : 1 * a = a
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ 1 * a = a
-- added: 2026-06-07
theorem my_one_mul {G : Type*} [Group G] (a : G) : 1 * a = a := by
  simp

-- stmt: theorem my_mul_inv_cancel {G : Type*} [Group G] (a : G) : a * a⁻¹ = 1
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ a * a⁻¹ = 1
-- added: 2026-06-07
theorem my_mul_inv_cancel {G : Type*} [Group G] (a : G) : a * a⁻¹ = 1 := by
  simp

-- stmt: theorem my_inv_mul_cancel {G : Type*} [Group G] (a : G) : a⁻¹ * a = 1
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ a⁻¹ * a = 1
-- added: 2026-06-07
theorem my_inv_mul_cancel {G : Type*} [Group G] (a : G) : a⁻¹ * a = 1 := by
  simp

-- stmt: theorem my_inv_inv {G : Type*} [Group G] (a : G) : a⁻¹⁻¹ = a
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ a⁻¹⁻¹ = a
-- added: 2026-06-07
theorem my_inv_inv {G : Type*} [Group G] (a : G) : a⁻¹⁻¹ = a := by
  simp

-- stmt: theorem my_mul_inv_rev {G : Type*} [Group G] (a b : G) : (a * b)⁻¹ = b⁻¹ * a⁻¹
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a b : G
--   ⊢ (a * b)⁻¹ = b⁻¹ * a⁻¹
-- added: 2026-06-07
theorem my_mul_inv_rev {G : Type*} [Group G] (a b : G) : (a * b)⁻¹ = b⁻¹ * a⁻¹ := by
  simp

-- stmt: theorem my_mul_left_cancel {G : Type*} [Group G] {a b c : G} (h : a * b = a * c) : b = c
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a b c : G
--   h : a * b = a * c
--   ⊢ b = c
-- added: 2026-06-07
theorem my_mul_left_cancel {G : Type*} [Group G] {a b c : G} (h : a * b = a * c) : b = c := by
  aesop

-- stmt: theorem my_mul_right_cancel {G : Type*} [Group G] {a b c : G} (h : a * b = c * b) : a = c
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a b c : G
--   h : a * b = c * b
--   ⊢ a = c
-- added: 2026-06-07
theorem my_mul_right_cancel {G : Type*} [Group G] {a b c : G} (h : a * b = c * b) : a = c := by
  aesop

-- stmt: theorem my_one_unique {G : Type*} [Group G] {e : G} (h : ∀ a : G, e * a = a) : e = 1
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   e : G
--   h : ∀ (a : G), e * a = a
--   ⊢ e = 1
-- added: 2026-06-07
theorem my_one_unique {G : Type*} [Group G] {e : G} (h : ∀ a : G, e * a = a) : e = 1 := by
  aesop

-- stmt: theorem my_eq_inv_mul_of_mul_eq {G : Type*} [Group G] {a b c : G} (h : a * b = c) : b = a⁻¹ * c
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a b c : G
--   h : a * b = c
--   ⊢ b = a⁻¹ * c
-- added: 2026-06-07
theorem my_eq_inv_mul_of_mul_eq {G : Type*} [Group G] {a b c : G} (h : a * b = c) : b = a⁻¹ * c := by
  aesop

-- stmt: theorem my_subgroup_one {G : Type*} [Group G] (H : Subgroup G) : (1 : G) ∈ H
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H : Subgroup G
--   ⊢ 1 ∈ H
-- added: 2026-06-07
theorem my_subgroup_one {G : Type*} [Group G] (H : Subgroup G) : (1 : G) ∈ H := by
  simp

-- stmt: theorem my_subgroup_inv {G : Type*} [Group G] {H : Subgroup G} {a : G} (ha : a ∈ H) : a⁻¹ ∈ H
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H : Subgroup G
--   a : G
--   ha : a ∈ H
--   ⊢ a⁻¹ ∈ H
-- added: 2026-06-07
theorem my_subgroup_inv {G : Type*} [Group G] {H : Subgroup G} {a : G} (ha : a ∈ H) : a⁻¹ ∈ H := by
  aesop

-- stmt: theorem my_subgroup_mul {G : Type*} [Group G] {H : Subgroup G} {a b : G} (ha : a ∈ H) (hb : b ∈ H) : a * b ∈ H
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H : Subgroup G
--   a b : G
--   ha : a ∈ H
--   hb : b ∈ H
--   ⊢ a * b ∈ H
-- added: 2026-06-07
theorem my_subgroup_mul {G : Type*} [Group G] {H : Subgroup G} {a b : G} (ha : a ∈ H) (hb : b ∈ H) : a * b ∈ H := by
  aesop

-- stmt: theorem my_subgroup_inter {G : Type*} [Group G] {H K : Subgroup G} {a : G} (ha : a ∈ H) (hb : a ∈ K) : a ∈ H ⊓ K
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H K : Subgroup G
--   a : G
--   ha : a ∈ H
--   hb : a ∈ K
--   ⊢ a ∈ H ⊓ K
-- added: 2026-06-07
theorem my_subgroup_inter {G : Type*} [Group G] {H K : Subgroup G} {a : G} (ha : a ∈ H) (hb : a ∈ K) : a ∈ H ⊓ K := by
  tauto

-- stmt: theorem lagrange {G : Type*} [Group G] (H : Subgroup G) : Nat.card H ∣ Nat.card G
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H : Subgroup G
--   ⊢ Nat.card ↥H ∣ Nat.card G
-- added: 2026-06-07
theorem lagrange {G : Type*} [Group G] (H : Subgroup G) : Nat.card H ∣ Nat.card G := by
  exact Subgroup.card_subgroup_dvd_card H

-- stmt: theorem lagrange_index {G : Type*} [Group G] (H : Subgroup G) : Nat.card H * H.index = Nat.card G
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   H : Subgroup G
--   ⊢ Nat.card ↥H * H.index = Nat.card G
-- added: 2026-06-07
theorem lagrange_index {G : Type*} [Group G] (H : Subgroup G) : Nat.card H * H.index = Nat.card G := by
  simp

-- stmt: theorem my_pow_orderOf_eq_one {G : Type*} [Group G] (a : G) : a ^ orderOf a = 1
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   ⊢ a ^ orderOf a = 1
-- added: 2026-06-07
theorem my_pow_orderOf_eq_one {G : Type*} [Group G] (a : G) : a ^ orderOf a = 1 := by
  norm_num

-- stmt: theorem my_orderOf_dvd_of_pow_eq_one {G : Type*} [Group G] {a : G} {n : ℕ} (h : a ^ n = 1) : orderOf a ∣ n
-- goal:
--   G : Type u_1
--   inst✝ : Group G
--   a : G
--   n : ℕ
--   h : a ^ n = 1
--   ⊢ orderOf a ∣ n
-- added: 2026-06-07
theorem my_orderOf_dvd_of_pow_eq_one {G : Type*} [Group G] {a : G} {n : ℕ} (h : a ^ n = 1) : orderOf a ∣ n := by
  exact orderOf_dvd_of_pow_eq_one h

-- stmt: theorem my_orderOf_dvd_card {G : Type*} [Group G] [Fintype G] (a : G) : orderOf a ∣ Fintype.card G
-- goal:
--   G : Type u_1
--   inst✝¹ : Group G
--   inst✝ : Fintype G
--   a : G
--   ⊢ orderOf a ∣ Fintype.card G
-- added: 2026-06-07
theorem my_orderOf_dvd_card {G : Type*} [Group G] [Fintype G] (a : G) : orderOf a ∣ Fintype.card G := by
  exact orderOf_dvd_card

-- stmt: theorem my_pow_card_eq_one {G : Type*} [Group G] [Fintype G] (a : G) : a ^ Fintype.card G = 1
-- goal:
--   G : Type u_1
--   inst✝¹ : Group G
--   inst✝ : Fintype G
--   a : G
--   ⊢ a ^ Fintype.card G = 1
-- added: 2026-06-07
theorem my_pow_card_eq_one {G : Type*} [Group G] [Fintype G] (a : G) : a ^ Fintype.card G = 1 := by
  norm_num

-- stmt: theorem my_ring_mul_add {R : Type*} [CommRing R] (a b c : R) : a * (b + c) = a * b + a * c
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a b c : R
--   ⊢ a * (b + c) = a * b + a * c
-- added: 2026-06-07
theorem my_ring_mul_add {R : Type*} [CommRing R] (a b c : R) : a * (b + c) = a * b + a * c := by
  ring

-- stmt: theorem my_ring_add_mul {R : Type*} [CommRing R] (a b c : R) : (a + b) * c = a * c + b * c
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a b c : R
--   ⊢ (a + b) * c = a * c + b * c
-- added: 2026-06-07
theorem my_ring_add_mul {R : Type*} [CommRing R] (a b c : R) : (a + b) * c = a * c + b * c := by
  ring

-- stmt: theorem my_ring_mul_comm {R : Type*} [CommRing R] (a b : R) : a * b = b * a
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a b : R
--   ⊢ a * b = b * a
-- added: 2026-06-07
theorem my_ring_mul_comm {R : Type*} [CommRing R] (a b : R) : a * b = b * a := by
  ring

-- stmt: theorem my_ring_mul_zero {R : Type*} [CommRing R] (a : R) : a * 0 = 0
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a : R
--   ⊢ a * 0 = 0
-- added: 2026-06-07
theorem my_ring_mul_zero {R : Type*} [CommRing R] (a : R) : a * 0 = 0 := by
  ring

-- stmt: theorem my_ring_zero_mul {R : Type*} [CommRing R] (a : R) : 0 * a = 0
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a : R
--   ⊢ 0 * a = 0
-- added: 2026-06-07
theorem my_ring_zero_mul {R : Type*} [CommRing R] (a : R) : 0 * a = 0 := by
  ring

-- stmt: theorem my_ring_neg_one_mul {R : Type*} [CommRing R] (a : R) : -1 * a = -a
-- goal:
--   R : Type u_1
--   inst✝ : CommRing R
--   a : R
--   ⊢ -1 * a = -a
-- added: 2026-06-07
theorem my_ring_neg_one_mul {R : Type*} [CommRing R] (a : R) : -1 * a = -a := by
  ring

end AutoProved
